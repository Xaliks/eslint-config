# [ESLing Config](https://www.npmjs.com/package/@xaliksss/eslint-config)

## Install

```
$ npm install --save-dev @xaliksss/eslint-config
```

## Usage

Use `extends` in your ESLint config

```json
{
  "extends": ["@xaliksss/eslint-config"]
}
```
